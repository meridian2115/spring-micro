server:
  port: 8888
spring:
  profiles:
    include: native
  native:
    searchLocations: classpath:/config
  application:
    name: config-server
management:
  endpoints:
    web:
      exposure:
        include: "*"